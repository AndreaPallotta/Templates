#!/bin/sh

# Check OS version

# Check if subversion is installed and install if not

# If package cannot be installed, exit with error message

# If no flag is passed, Require user input on what template to pull and where to save it

# Pull template

exit 0

